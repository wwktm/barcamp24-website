# Turso (libSQL) — call-for-speakers proposal storage.
# Server-side only: never prefix these with PUBLIC_ and never import them into a
# client component. Set the same two values in Netlify env for production.
#   turso db show barcamp --url
#   turso db tokens create barcamp --expiration none
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
